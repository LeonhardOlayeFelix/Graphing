<syncfusion:RibbonWindow x:Class="Graphing.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Graphing.Views"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf" 
        xmlns:syncfusionskin ="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
        xmlns:ViewModels="clr-namespace:Graphing.ViewModels"
        xmlns:View="clr-namespace:Graphing.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <syncfusion:RibbonWindow.Resources>
        <ViewModels:ViewModelLocator x:Key="Locator"/>
    </syncfusion:RibbonWindow.Resources>
    <syncfusion:RibbonWindow.DataContext>
        <Binding Path="MainViewModel" Source="{StaticResource Locator}"/>
    </syncfusion:RibbonWindow.DataContext>
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <!--Footer-->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <View:RibbonHost Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3"/>

        

        <StackPanel Grid.Column="3" Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center">
            <Button Margin="5"  Width="50" Height="30"  Content="Light" Command="{Binding LightThemeCommand}"/>
            <Button Width="50" Margin="5" Height="30"  Content="Dark" Command="{Binding DarkThemeCommand}"/>
        </StackPanel>
    </Grid>
</syncfusion:RibbonWindow>
