<syncfusion:RibbonWindow x:Class="Graphing.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Graphing.Views"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:syncfusionskin ="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
        xmlns:ViewModels="clr-namespace:Graphing.ViewModels"
        xmlns:View="clr-namespace:Graphing.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <syncfusion:RibbonWindow.Resources>
        <ViewModels:ViewModelLocator x:Key="Locator"/>
    </syncfusion:RibbonWindow.Resources>
    <syncfusion:RibbonWindow.DataContext>
        <Binding Path="MainViewModel" Source="{StaticResource Locator}"/>
    </syncfusion:RibbonWindow.DataContext>
    <Grid>

        <View:RibbonHost/>

        <!--<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Button Width="50" Height="30"  Content="Click me" Command="{Binding LightThemeCommand}"/>
            <Button Width="50" Height="30"  Content="Click me" Command="{Binding DarkThemeCommand}"/>
        </StackPanel>-->
    </Grid>
</syncfusion:RibbonWindow>
